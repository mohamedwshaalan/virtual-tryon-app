TODO
=====
1. Move rating env on this PC []
2. HOOD: Storing outfit in db if collision already generated, else, generate the collision []
3. HOOD: Storing the pickle dump? []
4. Script to run all servers user/models []
5. Implement DB usage in model APIs (insertion and getting consistent under all apis)
    - Model servers to utilise same db instance as main server
    - MICE: retrieve gender from user table (modify table to have gender)


DB
===
1. Add gender in user table
2. Add item_link in item table